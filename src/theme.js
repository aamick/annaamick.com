import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#00bcd4'
    },
    secondary: {
      main: '#ff5252'
    },
    background: {
      default: '#445963'
    }
  }
})

export default theme
